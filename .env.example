# ------------------------------------------------------------
# Core App
# ------------------------------------------------------------
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace_with_long_random_string

# ------------------------------------------------------------
# Database (Neon Postgres)
# IMPORTANT:
# - DATABASE_URL can be pooled or direct
# - DIRECT_URL should be DIRECT / NON-POOLED (best for migrations)
# ------------------------------------------------------------
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
DIRECT_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

# ------------------------------------------------------------
# Cron protection
# Call: /api/cron/refresh?secret=YOUR_CRON_SECRET
# ------------------------------------------------------------
CRON_SECRET=replace_with_long_random_string

# ------------------------------------------------------------
# Provider
# ------------------------------------------------------------
POLYMARKET_API_BASE=https://gamma-api.polymarket.com

# ------------------------------------------------------------
# Optional Redis (Upstash) - only required if you use cache/rateLimit via Upstash
# ------------------------------------------------------------
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ------------------------------------------------------------
# Optional GitHub OAuth (only required if GitHub login is enabled)
# ------------------------------------------------------------
GITHUB_ID=
GITHUB_SECRET=
